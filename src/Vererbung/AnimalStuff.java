package Vererbung;

// in interface, every method is considered abstract
// also all fields are final = not changeable (not recommended to use in interface)
public interface AnimalStuff {

    public void makeSound();
}
